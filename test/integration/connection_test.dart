import 'package:flutter_test/flutter_test.dart';

/// Placeholder integration tests.
///
/// These require a running device/emulator and the full app stack.
/// Implement with `integration_test` package and `flutter drive`.
void main() {
  group('Connection flow (placeholder)', () {
    test('placeholder — connect to server', () async {
      // TODO: Use integration_test to:
      //   1. Launch the app
      //   2. Select a server
      //   3. Tap connect
      //   4. Verify connection state changes
      expect(true, isTrue);
    });

    test('placeholder — disconnect from server', () async {
      // TODO: Verify disconnect flow
      expect(true, isTrue);
    });

    test('placeholder — subscription import', () async {
      // TODO: Import a subscription URL and verify servers appear
      expect(true, isTrue);
    });

    test('placeholder — settings persistence', () async {
      // TODO: Toggle a setting and verify it persists across restarts
      expect(true, isTrue);
    });
  });
}
